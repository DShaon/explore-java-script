const lyrics = 'tumi Bondhu kala pakhi. Sada sada kala kalaRong. Jomeche Sada kala Ala BaAla KALa'
const parts = lyrics.split(' ')
const sentences = lyrics.split('.')
const characterSplit = lyrics.split(' ')
console.log(characterSplit)


// slice
const partialSlice = lyrics.slice(5,22) /* (start index, end index) */
console.log(partialSlice)

// substring and slice are same

// concat 

